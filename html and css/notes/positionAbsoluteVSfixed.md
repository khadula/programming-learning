- for position fixed, the elements are placed in the browser window
- but for position absolute, the elements are placed on the page

what this means is that when you scroll, the page doesnt move - only the browser window does (what you see)

- absolute is mainly useful for going inside other elements and making precise stuff

- if youre gonna use position absolute on a non fixed element, use position relative on the parent